1. Создание карточки продукта (CreateProductTest)
    1.1. createProductInFoodCategoryTest()
        - Отправка запроса на создание карточки продукта
            - Получен успешный код ответа
            - Полученный id карточки не пустое значение
        - Отправка запроса на получение карточки продукта по id
            - Получен успешный код ответа
            - Полученный id карточки совпадает с полученным ранее
            - Значения полей title, categoryTitle, price совпадают с теми, что передавали на предыдущем этапе
        - Отправка запроса на удаление карточки продукта
            - Получен успешный код ответа

    1.2 createProductWithExtraFieldTest()
        - Отправка запроса на создание карточки продукта с полем id
            - Получен неуспешный код ответа

2. Получение информации по карточкам (GetProductTest)
    2.1. getProductByIdTest()
        - Отправка запроса на получение карточки продукта по существующему id
            - Получен успешный код ответа
            - Полученный id карточки совпадает с полученным ранее
            - Поля title, categoryTitle, price содержат не пустое значение

    2.2. getUnknownProductByIdTest()
        - Отправка запроса на получение карточки продукта по несуществующему id
                - Получен неуспешный код ответа (404)

    2.3. getAllProductsTest()
         - Отправка запроса на получение списка всех карточек продуктов
            - Получен успешный код ответа
            - Размер списка больше 1
            - Проверить значения полей title, categoryTitle, price для 1 элемента

3. Изменение информации в карточках продуктов (ModifyProductTest)
    3.1. updateProductInFoodCategoryTest()
        - Отправка запроса на создание карточки продукта
            - Получен успешный код ответа
            - Полученный id карточки не пустое значение
        - Отправка запроса на изменение данных в карточке продукта
            - Получен успешный код ответа
            - Тело ответа содержит обновленные значения полей title, categoryTitle, price
        - Отправка запроса на удаление карточки продукта
            - Получен успешный код ответа

    3.2. updateUnknownIdProductTest()
         - Отправка запроса на изменение данных в карточке продукта с несуществующим id
             - Получен неуспешный код ответа (400)